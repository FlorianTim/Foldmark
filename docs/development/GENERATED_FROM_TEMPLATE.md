# Generated from the LumbreCode Web App Template

> **In the template repository this file is the blank form.** The Template Initializer fills it in
> the concrete app and never deletes it. In the app it answers "what did this come from, and what
> has it inherited since?".

## Provenance

|                  |                                                                          |
| ---------------- | ------------------------------------------------------------------------ |
| Template         | `lambro-web-app-template` (LumbreCode)                                   |
| Template version | `<VERSION / template.config.json → template.version at generation time>` |
| Template commit  | `<git rev-parse --short HEAD of the template>`                           |
| Generated on     | `<YYYY-MM-DD>`                                                           |
| Generated by     | `<Template Initializer agent / manual>`                                  |
| App identity     | `template.config.json → application`                                     |
| Input material   | `drafts/init/` — see `drafts/init/processed.md`                          |

The app's own version starts at **`0.1.0`** and has nothing to do with the template's.
`CHANGELOG.md` and `docs/product/FEATURE_LOG.md` are reset; this table is the only record of what
came before.

## What was replaced

| Path                                                               | Replaced by                       |
| ------------------------------------------------------------------ | --------------------------------- |
| `src/domain/todo/`                                                 | `<the app's domain>`              |
| `src/application/{ports,usecases,errors}` demo files               | `<the app's ports and use cases>` |
| `src/infrastructure/db/DexieTodoRepository.ts`                     | `<the app's adapter>`             |
| `src/presentation/components/TodoPanel.vue`, `stores/todoStore.ts` | `<the app's views and stores>`    |
| demo tests and `todo.*` i18n keys                                  | `<the app's tests and copy>`      |

## What was inherited and kept

The app shell, unchanged in structure:

- `SettingsPanel.vue`, `AboutPanel.vue`, `PrivacyNotice.vue`, `SafeMarkdown.vue`
- `browserStorage`, `composables/useTheme`, `i18n`, `markdown`
- `config/`, `domain/common/`, `styles/`, `main.ts`, `app/compositionRoot.ts`
- `infrastructure/db/AppDb.ts` — the database, its versioning and migrations
- the whole toolchain: `.github/`, `scripts/`, `compliance/`, `config/`

Per-app content that **was** rewritten: the i18n copy, the help documents, the privacy text,
`template.config.json`, and the nav entries in `App.vue`.

## Template updates adopted since

Append a row whenever a fix or feature from a newer template version is pulled into this app.
Without it, nobody can tell which template improvements this app already has.

| Date | Template version | What was adopted | Notes |
| ---- | ---------------- | ---------------- | ----- |
|      |                  |                  |       |

## Feedback owed back to the template

Problems found while building this app go in `docs/template-feedback/` via the
`capture-template-feedback` skill, and are filed back against the template repository. That
directory is why the next app starts better than this one did.
