# Generated from the LumbreCode Web App Template

What this application came from, and what it has inherited since. **Never delete this file.**

## Provenance

|                  |                                                                  |
| ---------------- | ---------------------------------------------------------------- |
| Template         | `lambro-web-app-template` (LumbreCode)                           |
| Template version | `1.0.0`                                                          |
| Template commit  | `0f67fa3` (initial commit of this repository)                    |
| Generated on     | 2026-09-11                                                       |
| Generated by     | Template Initializer (Claude Opus 5, Claude Code)                |
| App identity     | `template.config.json → application` (Foldmark, slug `foldmark`) |
| Input material   | `drafts/init/` — see `drafts/init/processed.md`                  |

The app's own version (`1.0.0` for the first release) has nothing to do with the template's.
`CHANGELOG.md` and `docs/product/FEATURE_LOG.md` were reset; this table is the only record of what
came before.

## What was replaced

| Path                                                                               | Replaced by                                                                                                                                                                        |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/domain/todo/`                                                                 | `src/domain/{common,document,print,address,asset,email,export,markdown}/`                                                                                                          |
| `src/application/{errors/TodoErrors,ports/TodoRepository,usecases/TodoService}.ts` | Foldmark ports (`DocumentRepository`, `LibraryRepositories`, `MarkdownDocumentCodec`, `EmailPorts`, `ImageProbe`, `SettingsStore`, `BackupPorts`) and six use cases                |
| `src/infrastructure/db/DexieTodoRepository.ts`                                     | `DexieDocumentRepository`, `DexieLibraryRepositories`, plus codec, email and asset adapters                                                                                        |
| `src/presentation/components/TodoPanel.vue`, `stores/todoStore.ts`                 | `src/presentation/components/foldmark/*`, `stores/{workspace,library}Store.ts`                                                                                                     |
| `tests/{todo,todoService,dexieTodoRepository}.test.ts`, `tests/e2e/todo.spec.ts`   | `tests/{units,printProfiles,frontMatter,markdownCodec,renderPlan,validateForTarget,email,services}.test.ts`, `tests/security/untrustedInput.test.ts`, `tests/e2e/foldmark.spec.ts` |
| `todo.*` i18n keys                                                                 | Foldmark copy in German and English                                                                                                                                                |
| `openspec/specs/`, `openspec/changes/`                                             | Foldmark capability specs and `0001-foldmark-mvp`                                                                                                                                  |

## What was inherited and kept

The app shell, unchanged in structure:

- `SettingsPanel.vue`, `AboutPanel.vue`, `PrivacyNotice.vue`, `SafeMarkdown.vue`, `HelpPanel.vue`,
  `ProPanel.vue`, `DiagnosticsPanel.vue`
- `browserStorage`, `composables/useTheme`, `i18n`, `settings/settingsRegistry`
- `config/`, `domain/common/Result.ts`, `styles/`, `main.ts`, `app/compositionRoot.ts`
- `infrastructure/db/AppDb.ts` — the database, its versioning and migrations
- the whole toolchain: `.github/`, `scripts/`, `compliance/`, `config/`, `.lumbrecode/`

Adapted rather than replaced:

| Path                                                              | Adaptation                                                                                                     |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `src/App.vue`                                                     | Foldmark navigation and views; the shell, intro and settings routing stay                                      |
| `src/app/compositionRoot.ts`                                      | Foldmark services; the composition pattern and the entitlement seam stay                                       |
| `src/infrastructure/db/AppDb.ts`                                  | Foldmark tables replace the demo table                                                                         |
| `src/presentation/components/PrivacyPanel.vue`                    | Gained the counted inventory and backup; both destructive actions stay separate                                |
| `src/presentation/settings/settingsRegistry.ts`                   | Four Foldmark preferences added; theme default now follows the configured theme                                |
| `src/presentation/markdown/parseMarkdown.ts`                      | Re-exports the parser, which moved to the domain for a second consumer                                         |
| `src/styles/app.css`                                              | Demo-specific layout rules replaced; Foldmark screens live in `foldmark.css` and `print.css`                   |
| `eslint.config.js`                                                | Browser globals added for the debounced editor, resize observation, the unsaved-changes guard and image import |
| `vite.config.ts`                                                  | Dev-only `style-src` relaxation so the dev server can show stylesheets (ADR 0013)                              |
| `tests/{appConfig,publicResources,settingsRegistry,i18n}.test.ts` | Identity expectations updated; i18n gained catalogue-parity tests                                              |

## Template updates adopted since

| Date | Template version | What was adopted | Notes |
| ---- | ---------------- | ---------------- | ----- |
|      |                  |                  |       |

## Feedback owed back to the template

See `docs/template-feedback/`.
