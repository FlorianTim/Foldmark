# Agentic Initialization

## Requirement

This repository was generated from the LumbreCode Web App Template. Its provenance is recorded in
`docs/development/GENERATED_FROM_TEMPLATE.md` and must not be deleted.

The input material lives in `drafts/init/` and is non-normative. `drafts/init/processed.md` records,
per item, what it was turned into; the folder may be retired only when every row says `done` or
`dropped`.

Problems found in the template while building this application are recorded in
`docs/template-feedback/` as they happen, including the ones that were worked around.

## Verification

- `npm run docs:check` and `npm run openspec:check` cover the required documents.
- `npm run version:check` covers the separation of template and application versions.
