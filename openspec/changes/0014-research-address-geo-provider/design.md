# Design: Research an optional address/geo provider

No design until the research concludes. The evaluation table lives in `docs/privacy/` alongside the
external request register, so the outcome is visible where the privacy review is done.
