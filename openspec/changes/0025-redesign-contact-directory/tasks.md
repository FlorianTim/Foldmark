# Tasks: Contact directory

- [x] Domain: `PostalEntry`, `ContactPoint`, name fields, projections (`normalizeContact`), fuzzy
      search, pagination helper; schemas with defaults; tests incl. 1.0 fixture.
- [x] Dexie v4 (shared with 0024); search tokens include every address and email.
- [x] `CountryCombobox.vue`; recipient form uses it.
- [x] `ContactDialog.vue`; `AddressBookPanel.vue` → list with search, groups, pagination, actions.
- [x] Wording "Contact directory"; snapshot from the primary address; i18n; e2e; FEATURE_LOG;
      `address-book` in `document-model.md` / `local-storage.md`.
