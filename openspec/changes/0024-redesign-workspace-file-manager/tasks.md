# Tasks: File manager

- [x] Domain: `Folder`, document fields `folderId`, `archived`, `lastOpenedAt`; schemas; codec
      ignores them (local organisation, not part of the portable file).
- [x] Dexie v4 upgrade; `DexieFolderRepository`; `FolderService`; document service archive/move.
- [x] `DocumentListPanel.vue`: toolbar, new menu, table, sorting, breadcrumb, archive view, row
      actions, rename dialog, move dialog.
- [x] Backup includes folders; privacy inventory counts them.
- [x] i18n; tests; e2e; FEATURE_LOG; `local-storage.md`, `document-model.md`.
