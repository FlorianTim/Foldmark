# Tasks: Drag-and-drop document import

- [x] Drop zone overlay and file-type refusal in `DocumentListPanel.vue`.
- [x] `ImportPreviewDialog.vue` used by both entry points; queue for several files.
- [x] e2e: drop a letter (Playwright `setInputFiles` + synthetic `drop`), refuse a PNG.
- [x] `document-model.md` import section, FEATURE_LOG, CHANGELOG, help guide.
