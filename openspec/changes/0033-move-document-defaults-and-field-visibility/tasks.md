# Tasks: Document defaults and snapshots

- [x] Alias selects out of `MetadataForm`; `documentDefaults.aliases`; Settings → Colours; new
      documents inherit; tests.
- [x] Format → Document font… (dispatcher `theme:font`).
- [x] Spec wording for the three visibility kinds (a plan-equality test would be tautological: the
      accordion state is a UI preference the render input never sees).
- [x] `recipientContactId` in model, schema, codec; stale status and reset for sender and recipient;
      e2e.
- [x] `document-model.md`, `app-shell.md` (settings), FEATURE_LOG, CHANGELOG.
