# Tasks: Clear formatting and editor controls

- [x] `clearFormattingCommand` in the editor syntax; port command; toolbar button; menu entry;
      source-view variant; tests.
- [x] `EditorModeSwitch.vue`; undo/redo in the switch row; hint as tooltip. The round-trip warning
      line is not built: the editor already normalises on load and the fixtures pin every accepted
      normalisation, so a warning would fire on spellings the format documents as equivalent.
- [x] Control-size tokens; `AppIcon size="md"`; toolbar, workspace bar and preview controls on them.
- [x] Tooltip pass over icon-only controls; e2e accessible-name check.
- [x] `document-formatting.md`, FEATURE_LOG, CHANGELOG, screenshots.
