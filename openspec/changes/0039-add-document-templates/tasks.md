# Tasks: Document templates

- [x] `DocumentTemplate.ts` (record, `createTemplateFromDocument`, `documentFromTemplate`), schema,
      bounds; unit tests.
- [x] `premiumFeatures.ts` registry and `featureState`; unit tests.
- [x] `TemplateRepository` port, Dexie table (db v5), `TemplateService` with the gate; backup
      export/import/inventory/deletion; tests.
- [x] `workspaceStore.createFromTemplate`; `libraryStore.templates`; "+ New" entries;
      `SaveTemplateDialog.vue`; `TemplatesDialog.vue`; File menu entry; settings deletion; privacy
      inventory; i18n.
- [x] e2e: save → new from template → rename → delete; backup round trip with a template.
- [x] Specs (`document-model.md`, `local-storage.md`, `app-shell.md`), data inventory, FEATURE_LOG,
      CHANGELOG, help guide, roadmap R15-009.
