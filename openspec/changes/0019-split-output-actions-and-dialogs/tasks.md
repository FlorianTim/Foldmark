# Tasks: Output actions and dialogs

- [x] `AppDialog.vue` with focus return, Escape, backdrop, title, close; CSS incl. phone sheet.
- [x] `PrintDialog.vue`, `ExportDialog.vue`, `EmailDialog.vue`, `DocumentCheckDialog.vue`; remove
      `OutputDialog.vue`; workspace bar actions with icons and check badge.
- [x] `validateForTarget`: recipient missing is a warning for print/PDF; tests.
- [x] Email recipient from contact; missing address is a warning; mailto opens without `to`.
- [x] Preview quick print and maximize buttons.
- [x] i18n de/en; e2e updated (`openOutput` helpers); FEATURE_LOG; specs `render-and-print.md`,
      `email-handoff.md`, `validation.md`, `app-shell.md`.
