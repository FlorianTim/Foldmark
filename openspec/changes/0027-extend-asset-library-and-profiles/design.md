# Design: Assets and profiles

| Decision                                          | Alternative rejected | Why                                                                          |
| ------------------------------------------------- | -------------------- | ---------------------------------------------------------------------------- |
| `title` next to `label`, `label` read as fallback | Rename `label`       | Stored records and backups carry `label`; the schema maps it once.           |
| Profile edit limited to name and margins          | A full marker editor | R02-001 remains its own change; margins are what a custom paper needs first. |
