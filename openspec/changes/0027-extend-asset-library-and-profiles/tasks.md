# Tasks: Assets and profiles

- [x] Asset `title`/`description`/`qr-code` kind; service `updateMetadata`; panel editing; picker.
- [x] Profiles: names, grouping, six new built-ins, custom profile editor (name, margins), delete
      guard; document select grouped with `<optgroup>`.
- [x] i18n; tests; FEATURE_LOG; `print-profiles.md`, `assets-and-signatures.md`.
