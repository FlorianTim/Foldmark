# Tasks: Save as, duplicate, leave guard, context menu

- [ ] `DocumentService.saveAs` with the checkpoint; `workspaceStore.saveAs` / `duplicate`; unit
      tests.
- [ ] `SaveAsDialog.vue`, File menu entries, `saveAs` shortcut, i18n.
- [ ] `leaveGuard.ts` (history entry, `popstate`, `beforeunload`, `pagehide`) with fake-window
      tests; reopen on load from `history.state`.
- [ ] `ContextMenu.vue` on the command dispatcher; `contextmenu` handling in both editing surfaces;
      Paste through the Clipboard API with the disabled hint.
- [ ] e2e journeys; specs `app-shell.md`, `local-storage.md`; roadmap R17-007…R17-010; feature log;
      changelog; help guide.
