# Tasks: Editor toolbar

- [x] Mode switch first; shared toolbar; source-mode text commands; disabled states.
- [x] H1–H6 in port, adapter (`MarkdownHeadingLevel` 1–6), renderers, CSS.
- [x] Colour and highlight popovers on `AppPopover.vue` inside the toolbar, `TablePopover.vue`,
      `LinkDialog.vue`, `ImageDialog.vue` (library + import + preview).
- [x] Context flags in `selectionState`; toolbar disables; tests.
- [x] Icons; i18n; e2e; FEATURE_LOG; `document-formatting.md`.
