# Tasks: Demo data

- [x] `demoData` on documents, addresses, assets, folders (schemas, codec ignores it).
- [x] `DemoDataService` + fixtures module; repository `deleteDemo` helpers.
- [x] Settings action (dev only); i18n; tests; FEATURE_LOG.
