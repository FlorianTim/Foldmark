# Tasks: Command wiring and highlight

- [x] Function refs for `editor` and `preview` in `WorkspacePanel.vue`.
- [x] `commandDispatcher.ts` with target tracking; menu `disabled` from `canExecute`; keys and
      toolbar through the same object.
- [x] `printOptions.subjectBold` in model, schema, codec, paper CSS; Format → Bold on the subject.
- [x] `colorOf` refuses inline-directive names; parser and round-trip tests.
- [x] e2e: Format → Bold on a selection; the A9 highlight chain.
- [x] `document-formatting.md`, `document-model.md`, FEATURE_LOG, CHANGELOG.
