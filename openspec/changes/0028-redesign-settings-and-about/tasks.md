# Tasks: Settings and About

- [x] `SettingsPanel.vue` categories + navigation; `documentDefaults.ts`; settings entries.
- [x] Data management actions in `BackupService` (`deleteDocuments`, `deleteAddresses`,
      `deleteAssets`, `deleteHistory`); confirmations.
- [x] `licenses:generate` writes `THIRD-PARTY-NOTICES.generated.json` (runtime/development flag);
      `OpenSourcePanel.vue` table; About view (delivered with change 0018).
- [x] i18n; tests; FEATURE_LOG; `app-shell.md`, `local-storage.md`.
