# Tasks: Contact import

- [x] `domain/address/import/`: vCard reader, CSV reader, detection, limits; fixtures and tests.
- [x] `reviewImport` with the four signals and `mergeContact`; tests.
- [x] `AddressBookService.importContacts`; provenance.
- [x] `ContactImportDialog.vue`; button and drop zone in `AddressBookPanel.vue`; i18n; e2e.
- [x] `local-storage.md` contact section, threat model (untrusted files), FEATURE_LOG, CHANGELOG,
      data inventory.
